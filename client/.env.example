# Vite environment variables
# Copy this file to .env (for local) or set these in your hosting provider's dashboard.
# IMPORTANT: Vite only exposes variables prefixed with VITE_

# Base URL of your API server (no trailing slash). The app will append /api automatically.
# Examples:
# VITE_API_BASE_URL=http://localhost:4000
# VITE_API_BASE_URL=https://your-backend.example.com
VITE_API_BASE_URL=http://localhost:4000
