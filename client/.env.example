# Frontend environment variables (example)
# Copy to .env and adjust as needed for your environment

# Base URL of your backend API (no trailing /api)
# During local dev, the client defaults to window.location.origin so this can be omitted.
# For production builds, set to your deployed API origin, e.g. https://book-api.example.com
VITE_API_BASE_URL=
