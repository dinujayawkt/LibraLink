# Server environment variables (example)
# Copy this file to .env and fill in real values. NEVER commit .env to Git.

# App
PORT=4000
NODE_ENV=development
CLIENT_ORIGIN=http://localhost:5173
CORS_ORIGINS=

# Database
# For local dev you can use: mongodb://127.0.0.1:27017/library_app
# For Atlas, use a connection string WITHOUT credentials in the repo. Put the full URL here in your local .env
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>/<db>?retryWrites=true&w=majority

# Auth
# Generate a strong random string: e.g., `node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"`
JWT_SECRET=change_me
